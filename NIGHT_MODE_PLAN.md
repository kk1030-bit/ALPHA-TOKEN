# 夜間模式方案（停放，尚未實作）

最後更新：2026-07-10
狀態：**待辦 / 未啟用 / 不得自行下實盤單**

本文件是跨對話的專案記憶。日後討論或實作夜間模式前，先讀取本文件，避免依賴聊天上下文或摘要。

## 目標

使用者凌晨無法手動開倉，因此先驗證「夜間出現的訊號若依規則成交，結果是否真的有正期望」，而不是直接讓機器人拿真實資金自動交易。

## 第一階段：夜間影子交易（優先）

- 運作時段預設為台北時間 `23:00-08:00`。
- 只接受「底部啟動」或「軋空啟動」類型；強勢延續只列入觀察，不模擬追價進場。
- 初次訊號後等待 `5-15` 分鐘確認：價格完成回踩、OI 未轉弱、原始結構仍有效才建立紙上倉位。
- 訊號最久等待 `45` 分鐘，未確認即標記過期，不追價。
- 模擬成交必須計入手續費、滑價與 funding；TP/SL 的先後順序用最細可取得 K 線判定，同一根同時觸及則標記為歧義。
- 每晚最多同時一個模擬倉位，避免多個高度相關的山寨幣同時放大風險。
- 隔日上午產生報告：候選、未成交、已成交、過期、TP、SL、MFE、MAE、淨損益及觸發理由。
- 至少累積 `30-50` 筆有效樣本且扣除成本後期望值為正，才進入測試網。

預計指令：

- `/night on|off|status`
- `/morning`：查看最近一晚影子交易摘要

預計狀態機：

`候選 -> 等待確認 -> 模擬掛單/成交 -> TP / SL / 過期`

## 預設風控

- 僅底部啟動與軋空啟動可進入成交流程。
- 強勢延續訊號只通知，不自動進場。
- 每晚最多一個持倉。
- 每筆最大風險為帳戶淨值 `0.5%`。
- 當日累積虧損達帳戶淨值 `1%` 即停止建立新倉。
- 做多時 funding `> +0.10%` 一律阻擋。
- 禁止追價，確認逾時即放棄。
- 沿用策略管理假設：價格 `+10%` 先停利一半，其餘止損移到開倉價；價格 `-7%` 全部停損。實際下單數量必須依可承受損失反推，不能固定用保證金金額。

重要：`500 USDT` 保證金、`10x` 槓桿等於約 `5,000 USDT` 名目倉位。標的反向 `7%` 約虧損 `350 USDT`（尚未計費用），等於保證金的 `70%`，不適合作為夜間無人值守預設值。

## 第二階段：Binance Futures Testnet

- 第一階段通過後，先接 Binance Futures Testnet，不接真實資金。
- 使用程式建立訂單、交易所端 TP/SL、成交回報與斷線後對帳。
- 至少連續運作一週，比對理論價格、實際成交、滑價、漏單、重複單與重啟恢復狀況。
- 任一資料源逾時、狀態不一致或風控服務失效時，停止新倉並通知 Telegram。

## 第三階段：極小額實盤（需另行明確批准）

- 只有使用者日後明確批准，且測試網結果合格，才可開始。
- API Key 只能開交易權限，關閉提幣，設定 IP 白名單，存放於雲端秘密變數；不得貼在聊天、程式碼或 GitHub。
- 強制一倉上限、日損上限、全域停止開關、交易所端保護單及啟動時持倉對帳。
- 先以可忽略的小額運行，再依真實成交紀錄決定是否提高額度。

## 24/7 運作前提

- GitHub 只保存程式，不能讓 Telegram bot 或監控器持續執行。
- 電腦關機時仍要工作，必須部署到 Render、Railway、Fly.io 或 VPS 的常駐 worker。
- `data/` 的訊號、倉位與績效資料需要持久磁碟；沒有持久儲存就不能可靠恢復狀態。

## 實作紅線

- 本方案目前只保存，不啟用任何夜間交易功能。
- 未經使用者另行確認，不得建立真實 Binance 訂單。
- 尚未完成影子交易樣本與測試網驗證，不得直接跳到實盤。

官方實作參考：

- https://developers.binance.com/en/docs/introduction
- https://developers.binance.com/en/docs/products/derivatives-trading-usds-futures/quick-start
